from setuptools import find_packages, setup

setup(
    name='Medical-Chatbot',
    version='0.0.1',
    author='Hector S.',
    author_email='',
    install_requires = [],
    packages=find_packages()
)